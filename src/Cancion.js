// Clase que define una canción
// `nombre` = nombre de la canción
// `artista` = Artista o grupo que compuso la canción
// `letra` = letra de la canción
class Cancion {
  constructor(nombre, artista, letra) {
    this.nombre = nombre;
    this.artista = artista;
    this.letra = letra;
  }

  getNombre() {
    return true;
  }

  getLetra() {
    return true;
  }
}
